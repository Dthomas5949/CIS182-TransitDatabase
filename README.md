# CIS182-TransitDatabase
This is a database I made in SQL for a transit company. One file is the main query that creates the database and the other inserts values and has extensive select statements.
